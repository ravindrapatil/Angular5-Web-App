import { SimplePilePipe } from './simple-pile.pipe';

describe('SimplePilePipe', () => {
  it('create an instance', () => {
    const pipe = new SimplePilePipe();
    expect(pipe).toBeTruthy();
  });
});
