import { InjectdirectiveDirective } from './injectdirective.directive';

describe('InjectdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new InjectdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
